name: drift_libsql
description: "Turso/Libsql database client binding for Drift"
version: 0.1.0
homepage:
repository: https://github.com/dikatok/drift_libsql
issue_tracker: https://github.com/dikatok/drift_libsql/issues

environment:
  sdk: ^3.5.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  drift: ^2.26.1
  libsql_dart: ^0.9.0+0.9.30

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
